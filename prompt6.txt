You are a senior CPU architect and hardware/software co-design engineer.

I have built a custom 8-bit datapath, 16-bit address, multi-cycle RISC-style CPU in Verilog.

Current features:
- 8 general purpose registers (R0–R7)
- 16-bit instruction format
- 6-bit signed immediate
- Multi-cycle FSM (FETCH, DECODE, EXECUTE, WRITEBACK)
- ALU with flags: Z, C, N, V
- Arithmetic: ADD, SUB, AND, OR, XOR, ADDI
- Branch: BEQ, BNE, BLT (signed)
- JMP
- LOAD, STORE (absolute address via imm6)
- Harvard architecture
- 256-word instruction memory
- 256-byte data memory

Instruction Format:
[15:12] opcode
[11:9]  rd
[8:6]   rs
[5:0]   imm6 (signed for branch)

Branch semantics:
- PC increments in FETCH
- PC loads in WRITEBACK
- branch_target = pc_out + signed_offset
- BLT = negative XOR overflow

I want to formally move to Foundation Phase.

You must structure the response into 4 clearly separated sections:

==================================================
SECTION 1 — ISA FREEZE (Version v1.0)
==================================================

Tasks:
- Produce a finalized opcode table.
- Remove any unimplemented instructions.
- Define exact semantics for each instruction.
- Define flag behavior per instruction.
- Define branch timing precisely.
- Define memory addressing mode.
- Define instruction encoding in binary and hex.
- Provide a formal ISA v1.0 specification table.

Deliver:
- Clean, finalized ISA v1.0 definition.
- No speculative instructions.
- Explicit statement of what is NOT included.

==================================================
SECTION 2 — ASSEMBLER DESIGN
==================================================

Design a robust assembler in Python.

Requirements:
- Two-pass assembler.
- Label resolution.
- Signed 6-bit branch offsets.
- Register syntax R0–R7.
- Immediate syntax: #value.
- Range checking for imm6.
- Output format: .mem or .hex.
- Error reporting with line numbers.

Deliver:
- Encoding table.
- Two-pass algorithm description.
- Python implementation skeleton.
- Example assembly file.
- Example output.
- Strategy to load into instruction_memory.

==================================================
SECTION 3 — DEBUG BUS ARCHITECTURE
==================================================

Design a debug interface layer inside cpu_core.

Requirements:
- Non-intrusive.
- Synthesizable.
- Export:
    PC
    IR
    FSM state
    Register file snapshot
    Flags
- Optional memory peek.
- Optional single-step mode.
- Optional halt instruction.

Deliver:
- Debug bus signal definition.
- Verilog interface design.
- Clean integration strategy.
- Strategy for FPGA future.
- Strategy for Verilator access.

==================================================
SECTION 4 — VERILATOR SIMULATION FRAMEWORK
==================================================

Design a simulation ecosystem.

Goals:
- Convert CPU to C++ model via Verilator.
- Step cycle-by-cycle.
- Expose debug bus.
- Provide register dump.
- Allow single-step execution.
- Allow program load from assembler output.

Deliver:
- Verilator build command.
- Top-level wrapper design.
- C++ simulation loop.
- Suggested project folder structure.
- Clean separation between hardware model and GUI layer.
- Advice for scaling toward pipelined CPU later.

==================================================

Constraints:
- Do NOT rewrite CPU into single-cycle.
- Preserve multi-cycle FSM.
- Maintain synthesizable RTL.
- Focus on clean architectural progression.
- Be structured.
- Be formal.
- No casual language.
- No over-simplification.