You are acting as a senior CPU architect and technical documentation maintainer.

I am building a custom 8-bit datapath, 16-bit address, multi-cycle RISC-style CPU in Verilog.

Below is the current content of my cpu_architecture_spec.md file.

Your task is NOT to rewrite everything casually.

Your responsibilities:

1. Treat this file as a formal architecture specification.
2. Compare it against a realistic implementation of:
   - Multi-cycle FSM (FETCH, DECODE, EXECUTE, WRITEBACK)
   - Signed branch offsets (6-bit imm sign-extended)
   - BEQ, BNE, BLT
   - LOAD/STORE
   - JMP
   - 8 general registers
   - Harvard architecture
3. Identify inconsistencies between:
   - Spec and implementation
   - Spec and ISA table
   - Spec and branch behavior
4. Fix incorrect opcodes (example: PUSH/POP/CALL/RET are currently listed but not implemented).
5. Update instruction set table to reflect actual implemented instructions.
6. Clarify branch timing:
   - PC increment in FETCH
   - PC load in WRITEBACK
   - Relative branch target = pc_out + signed_offset
7. Add:
   - Signed offset explanation
   - BLT condition (negative XOR overflow)
   - Memory architecture details
   - LOAD/STORE addressing mode
8. Remove any instructions that are not implemented yet (PUSH, POP, CALL, RET unless explicitly implemented).
9. Add a new section:

   "Current Implementation Status"

   With:
   - Implemented features
   - Planned features
   - Not yet implemented

10. Keep it professional and technical.
    This document should read like a real ISA spec,
    not like a tutorial.

11. Maintain Markdown formatting cleanly.
12. Do NOT remove sections unnecessarily.
13. Preserve structure but improve correctness and clarity.
14. Add a version header at top:

    CPU Architecture Spec
    Version: v0.3 (Multi-cycle Branching + Memory Support)

Output:
- Fully updated cpu_architecture_spec.md
- Clean Markdown
- No casual explanations
- No extra commentary outside the file

Additionally:
- Add a formal opcode encoding table in binary and hex.
- Add a state transition table for the FSM.
- Add a control signal truth table for each instruction class.
- Add a section describing potential future pipelining.