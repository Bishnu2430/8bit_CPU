You are an expert hardware architect and Verilog designer.

I am building an 8-bit datapath + 16-bit address multi-cycle CPU in Verilog.

Project structure:

src/
- alu.v
- control_unit.v
- cpu_core.v
- data_memory.v
- decoder.v
- fetch_unit.v
- instruction_memory.v
- instruction_register.v
- program_counter.v
- reg_file.v

tb/
- cpu_core_tb.v

Architecture:
- Multi-cycle FSM (FETCH, DECODE, EXECUTE, WRITEBACK)
- Signed branch offsets (6-bit immediate sign-extended to 16-bit)
- Branch instructions:
  BEQ (1011)
  BNE (1100)
  BLT (1101)
- Arithmetic:
  ADD, SUB, AND, OR, XOR
  ADDI
- Memory:
  LOAD (0110)
  STORE (0111)
- Jump:
  JMP (1010)

Branch behavior:
- Comparison uses SUB in EXECUTE stage
- BEQ uses zero flag
- BNE uses !zero flag
- BLT uses negative_flag XOR overflow_flag
- Branch target = pc_out + signed_offset
- PC increments during FETCH

Current Issues:
1. Simulation appears to “hang” because program contains infinite backward loop.
2. Need architectural review for correctness of:
   - Multi-cycle timing
   - Branch timing (PC increment vs PC load)
   - Memory read/write timing
   - ALU control logic
   - Writeback mux behavior
3. Need verification that no combinational loops or race conditions exist.

Tasks:
- Review cpu_core.v and control_unit.v for architectural correctness.
- Identify any hidden timing hazards.
- Confirm proper multi-cycle sequencing.
- Suggest improvements for professional-grade structure.
- Recommend improvements for synthesis-readiness.
- Suggest improvements for FPGA deployment later.
- Suggest improvements for ISA extensibility.

Do NOT rewrite everything.
Instead:
- Identify precise improvements.
- Explain architectural risks.
- Suggest minimal clean fixes.
- Keep multi-cycle design.

Output:
- Structured technical review
- Explicit correction patches if needed
- Architectural commentary